$kind: http-request
url: "{{baseUrl}}/ticket/{{ticketId}}"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "jadwalId": {{jadwalId}}
    }
auth:
  type: bearer
  credentials:
    token: "{{adminToken}}"
order: 4000
